{
  "name": "ai-treehole-·-persona-simulator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:local": "vite build",
    "package:local": "npm run build && mkdir -p releases && ditto -c -k --keepParent dist releases/ai-treehole-local-dist.zip && zip -d releases/ai-treehole-local-dist.zip '*/._*' >/dev/null",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.30.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
